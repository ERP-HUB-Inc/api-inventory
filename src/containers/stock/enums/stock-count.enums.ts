export enum StockCountStatus {
  PARTIAL = "PARTIAL",
  FULL = "FULL"
};

export enum StockCountEntryStatus {
  COUNTED = "COUNTED",
  UNCOUNTED = "UNCOUNTED",
  INCLUDE = "INCLUDE",
  EXCLUDE = "EXCLUDE"
};